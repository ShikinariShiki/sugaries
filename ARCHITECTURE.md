# 🏗️ Sugaries Architecture - Web + Mobile

## System Architecture Diagram

```
┌─────────────────────────────────────────────────────────────────┐
│                         USER DEVICES                             │
└─────────────────────────────────────────────────────────────────┘
                               │
                ┌──────────────┼──────────────┐
                │              │              │
                ▼              ▼              ▼
        ┌─────────────┐ ┌─────────────┐ ┌─────────────┐
        │  Desktop    │ │   iPhone    │ │   Android   │
        │  Browser    │ │   App       │ │   App       │
        └─────────────┘ └─────────────┘ └─────────────┘
                │              │              │
                │              │              │
                ▼              ▼              ▼
        ┌─────────────────────────────────────────────┐
        │          FRONTEND APPLICATIONS              │
        ├─────────────────────────────────────────────┤
        │                                             │
        │  ┌───────────────┐    ┌──────────────────┐ │
        │  │   Web App     │    │   Mobile App     │ │
        │  │  (Next.js)    │    │  (React Native)  │ │
        │  │               │    │                  │ │
        │  │  - SSR/SSG    │    │  - Expo Router   │ │
        │  │  - React      │    │  - Native UI     │ │
        │  │  - Tailwind   │    │  - Animations    │ │
        │  │  - Framer     │    │  - Haptics       │ │
        │  └───────────────┘    └──────────────────┘ │
        │                                             │
        └─────────────────────────────────────────────┘
                               │
                               │ HTTPS/REST API
                               │
                               ▼
        ┌─────────────────────────────────────────────┐
        │          BACKEND SERVICES                    │
        ├─────────────────────────────────────────────┤
        │                                             │
        │  ┌─────────────────────────────────────┐   │
        │  │      Next.js API Routes             │   │
        │  │                                     │   │
        │  │  /api/auth/*    - Authentication   │   │
        │  │  /api/letter/*  - Letter CRUD      │   │
        │  │  /api/profile/* - User Management  │   │
        │  │  /api/upload/*  - File Uploads     │   │
        │  │                                     │   │
        │  └─────────────────────────────────────┘   │
        │                                             │
        │  ┌─────────────────────────────────────┐   │
        │  │      Business Logic Layer           │   │
        │  │                                     │   │
        │  │  - PIN Verification (bcrypt)       │   │
        │  │  - Shortcode Generation            │   │
        │  │  - Authentication (NextAuth)       │   │
        │  │  - Authorization Checks            │   │
        │  │                                     │   │
        │  └─────────────────────────────────────┘   │
        │                                             │
        └─────────────────────────────────────────────┘
                               │
                               │ Prisma ORM
                               │
                               ▼
        ┌─────────────────────────────────────────────┐
        │          DATABASE (PostgreSQL)               │
        ├─────────────────────────────────────────────┤
        │                                             │
        │  Tables:                                    │
        │  - users        (authentication)           │
        │  - letters      (content, metadata)        │
        │  - accounts     (OAuth providers)          │
        │  - sessions     (user sessions)            │
        │                                             │
        └─────────────────────────────────────────────┘
```

## Mobile App Architecture (Detailed)

```
┌────────────────────────────────────────────────────────────┐
│                    MOBILE APP LAYERS                        │
└────────────────────────────────────────────────────────────┘

┌────────────────────────────────────────────────────────────┐
│  PRESENTATION LAYER                                         │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  app/                    ← Screens (Expo Router)           │
│  ├── (auth)/                                               │
│  │   ├── signin.tsx     ← Sign in UI                      │
│  │   ├── signup.tsx     ← Sign up UI                      │
│  │   └── onboarding.tsx ← Welcome flow                    │
│  │                                                         │
│  ├── (app)/                                                │
│  │   ├── home.tsx       ← Dashboard                       │
│  │   ├── compose.tsx    ← Letter composition              │
│  │   └── profile.tsx    ← User profile                    │
│  │                                                         │
│  └── letter/                                               │
│      └── [code].tsx     ← Letter viewer                   │
│                                                             │
└────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────┐
│  COMPONENT LAYER                                            │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  src/components/         ← Reusable UI Components          │
│  ├── Button.tsx         ← Animated button                 │
│  ├── Envelope.tsx       ← Envelope animation              │
│  ├── PaperCard.tsx      ← Letter card                     │
│  └── PINInput.tsx       ← PIN entry                       │
│                                                             │
└────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────┐
│  STATE MANAGEMENT LAYER                                     │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  src/contexts/           ← React Context                   │
│  ├── AuthContext.tsx    ← User authentication state       │
│  └── MusicPlayerContext.tsx ← Music playback state        │
│                                                             │
└────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────┐
│  SERVICE LAYER                                              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  src/services/api.ts     ← API Integration                 │
│  ├── letterApi          ← Letter operations               │
│  ├── authApi            ← Authentication                  │
│  └── uploadApi          ← File uploads                    │
│                                                             │
│  HTTP Client: Axios with interceptors                      │
│  - Auto-add auth tokens                                    │
│  - Handle 401 errors                                       │
│  - Retry logic                                             │
│                                                             │
└────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────┐
│  STORAGE LAYER                                              │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  src/utils/storage.ts    ← Secure local storage           │
│  - SecureStore (encrypted)                                 │
│  - Token management                                        │
│  - User data caching                                       │
│                                                             │
└────────────────────────────────────────────────────────────┘
                               │
                               ▼
┌────────────────────────────────────────────────────────────┐
│  NATIVE LAYER                                               │
├────────────────────────────────────────────────────────────┤
│                                                             │
│  Expo Modules:                                             │
│  - SecureStore         ← Encrypted storage                │
│  - Haptics             ← Touch feedback                   │
│  - AV                  ← Audio/video                      │
│  - ImagePicker         ← Photo selection                  │
│  - Notifications       ← Push notifications               │
│  - Linking             ← Deep links                       │
│                                                             │
└────────────────────────────────────────────────────────────┘
```

## Data Flow

### Authentication Flow

```
1. User enters credentials
   │
   ▼
2. Mobile app validates input
   │
   ▼
3. POST /api/auth/login
   │
   ▼
4. Backend verifies credentials
   │
   ▼
5. JWT token generated
   │
   ▼
6. Token returned to mobile app
   │
   ▼
7. Token stored in SecureStore (encrypted)
   │
   ▼
8. User state updated (AuthContext)
   │
   ▼
9. Navigate to home screen
```

### Letter Viewing Flow

```
1. User opens letter link or enters code
   │
   ▼
2. GET /api/letter/:code
   │
   ▼
3. Backend returns letter metadata (no content!)
   │
   ▼
4. Show recipient name confirmation
   │
   ▼
5. User confirms identity
   │
   ▼
6. Envelope animation plays
   │
   ▼
7. If PIN required:
   │  │
   │  ▼
   │  POST /api/letter/:code/verify
   │  │
   │  ▼
   │  Backend verifies PIN
   │  │
   │  ▼
   │  Content returned
   │
   ▼
8. Letter content displayed with animations
```

### Letter Composition Flow

```
1. User fills composition form
   │
   ▼
2. Optional: Upload images
   │  │
   │  ▼
   │  POST /api/upload
   │  │
   │  ▼
   │  Image URL returned
   │
   ▼
3. POST /api/letter
   │
   ▼
4. Backend validates data
   │
   ▼
5. PIN encrypted (if provided)
   │
   ▼
6. Shortcode generated
   │
   ▼
7. Letter saved to database
   │
   ▼
8. Letter data returned with shortcode
   │
   ▼
9. Success message shown
   │
   ▼
10. User shares shortcode
```

## File Structure

```
mobile/
│
├── app/                    ← Screens (Expo Router)
│   ├── _layout.tsx        ← Root layout
│   ├── index.tsx          ← Entry point
│   ├── (auth)/            ← Auth group
│   │   ├── _layout.tsx
│   │   ├── signin.tsx
│   │   ├── signup.tsx
│   │   └── onboarding.tsx
│   ├── (app)/             ← Main app group
│   │   ├── _layout.tsx
│   │   ├── home.tsx
│   │   ├── compose.tsx
│   │   └── profile.tsx
│   └── letter/
│       └── [code].tsx     ← Dynamic route
│
├── src/
│   ├── components/        ← UI Components
│   │   ├── Button.tsx
│   │   ├── Envelope.tsx
│   │   ├── PaperCard.tsx
│   │   └── PINInput.tsx
│   │
│   ├── contexts/          ← State Management
│   │   ├── AuthContext.tsx
│   │   └── MusicPlayerContext.tsx
│   │
│   ├── services/          ← Backend Integration
│   │   └── api.ts
│   │
│   ├── utils/             ← Utilities
│   │   └── storage.ts
│   │
│   ├── config/            ← Configuration
│   │   └── environment.ts
│   │
│   └── types/             ← TypeScript Types
│       └── letter.ts
│
├── assets/                ← Static Assets
│   ├── icon.png
│   └── splash.png
│
├── app.json               ← Expo Config
├── eas.json               ← Build Config
├── package.json           ← Dependencies
├── tsconfig.json          ← TypeScript
└── babel.config.js        ← Babel
```

## Technology Stack

```
┌─────────────────────────────────────────────────┐
│              MOBILE STACK                        │
├─────────────────────────────────────────────────┤
│                                                  │
│  Framework:     React Native 0.73               │
│  Platform:      Expo 50                         │
│  Language:      TypeScript 5.3                  │
│  Router:        Expo Router (v3)                │
│  State:         React Context API               │
│  HTTP:          Axios                           │
│  Storage:       Expo SecureStore                │
│  Animations:    React Native Reanimated         │
│  Gestures:      React Native Gesture Handler   │
│  Navigation:    React Navigation Stack          │
│                                                  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              WEB STACK (Existing)                │
├─────────────────────────────────────────────────┤
│                                                  │
│  Framework:     Next.js 14                      │
│  Language:      TypeScript 5.3                  │
│  Styling:       Tailwind CSS                    │
│  Animations:    Framer Motion                   │
│  Auth:          NextAuth.js                     │
│                                                  │
└─────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────┐
│              BACKEND STACK (Shared)              │
├─────────────────────────────────────────────────┤
│                                                  │
│  Runtime:       Node.js                         │
│  Framework:     Next.js API Routes              │
│  ORM:           Prisma                          │
│  Database:      PostgreSQL                      │
│  Auth:          JWT + bcrypt                    │
│  Hosting:       Vercel                          │
│                                                  │
└─────────────────────────────────────────────────┘
```

## Deployment Architecture

```
┌─────────────────────────────────────────────────┐
│                 DEPLOYMENT                       │
└─────────────────────────────────────────────────┘

Web App:
  Code → GitHub → Vercel → Production
  └─ Automatic deployment on push

Mobile App:
  Code → EAS Build → Binaries
  └─ Manual build process
     ├─ Android → Google Play Store
     └─ iOS → Apple App Store

Backend:
  Next.js API → Vercel Edge Functions
  Database → PostgreSQL (Vercel/Supabase/etc.)
```

## Security Architecture

```
┌─────────────────────────────────────────────────┐
│                 SECURITY LAYERS                  │
└─────────────────────────────────────────────────┘

1. Transport Layer
   └─ HTTPS only (TLS 1.3)

2. Authentication Layer
   ├─ JWT tokens (signed)
   ├─ Bcrypt password hashing
   └─ Secure token storage (encrypted)

3. Authorization Layer
   ├─ Route guards (middleware)
   ├─ API route protection
   └─ User ownership checks

4. Data Layer
   ├─ PIN encryption (bcrypt)
   ├─ Prepared statements (SQL injection protection)
   └─ Input validation

5. Storage Layer
   └─ Expo SecureStore (hardware-backed encryption)
```

---

This architecture provides:
- ✅ Scalability
- ✅ Security
- ✅ Maintainability
- ✅ Cross-platform support
- ✅ Clean separation of concerns
